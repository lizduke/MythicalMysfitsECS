apiVersion: v1
kind: Service
metadata:
  name: "service-mythicalapi"
  namespace: "mythical-api"
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
  selector:
    app: "mythical-api"
